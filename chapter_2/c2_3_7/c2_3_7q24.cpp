//
// Created by Hleonor on 2023-08-15.
//
#include "chapter_2_header.h"

// 答案的做法是空间复杂度O(n)
// 那就没什么稀奇的了，不写了，因为只需要用一个长度为n的数组来记录每个元素是否出现过就行
// 遍历的过程中需要用到前驱指针进行删除节点的操作
void keep_one_absolute(ListNode* head)
{
    ListNode* cur = head->next;
    while ()
}

int main()
{
    return 0;
}